pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
        // Needed for StateProgressBars
        jcenter()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        jcenter()
    }
}
rootProject.name = "sopro-android-11-demo"
include ':app'
